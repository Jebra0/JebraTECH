extend type Query {
    GetNumOfReports(blog: Int! @rules(apply: ["required", "exists:blogs,id"])): Int!
}

type UserReport {
    id: ID!
    user_id: Int!
    blog_id: Int!
    created_at: DateTime!

    blog: Blog! @belongsTo
    user: [User]! @hasMany

}


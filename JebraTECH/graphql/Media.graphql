extend type Query {

    GetMediToBlog(blog_id: Int! @rules(apply: ["required", "exists:blogs,id"])): [Media]


}

type Media {
    id: ID!
    caption: String
    blog_id: Int!
    url: String!
    created_at: DateTime!
    updated_at: DateTime!

    blog: Blog! @belongsTo
}


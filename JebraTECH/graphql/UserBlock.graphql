extend type Query {
    GetNumOfBlocks(writer: Int!): Int!  # writer == user_blocked_id
}

type UserBlock {
    id: ID!
    user_id: Int!
    user_blocked_id: Int!
    created_at: DateTime!

    blocks: [User]! @belongsToMany
}


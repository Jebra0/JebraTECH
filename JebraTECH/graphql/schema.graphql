
type Query {
    user(id: ID @eq): User @find

    users: [User] @paginate
}

type Mutation {
    Login(name: String!, password: String!): String
}

type User {
    id: ID!
    name: String!
    email: String!
    is_writer: Boolean!
    gender: Boolean
    photo: String
    about: String
}
